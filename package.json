{
  "name": "sprite-to-mesh-hdr",
  "version": "0.1.0",
  "description": "Automation pipeline: Sprite sheet → Mesh geometry → HDR environment/light probe refinery",
  "main": "src/index.ts",
  "bin": {
    "sprite-to-mesh-hdr": "./src/cli.ts"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "pipeline": "node dist/cli.ts"
  },
  "dependencies": {
    "@gltf-transform/core": "^4.3.0",
    "commander": "^12.0.0",
    "fs-extra": "^11.2.0",
    "pngjs": "^7.0.0"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^20.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "files": [
    "src",
    "templates",
    "dist"
  ],
  "engines": {
    "node": ">=20"
  }
}
